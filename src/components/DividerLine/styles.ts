import styled from 'styled-components'

const Container = styled.div`
  width: 100%;
  height: 1px;
  background-color: rgba(222, 225, 233, 0.5);
  margin-left: 30px;
`

const Styles = {
  Container,
}

export default Styles
