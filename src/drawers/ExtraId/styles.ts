import styled from 'styled-components'

const Row = styled.div`
  margin: 10px 0 0 0;
`

const ExtraId = styled.p`
  margin: 0;
  font-size: 16px;
  line-height: 23px;
  color: #3fbb7d;
`

const Actions = styled.div`
  margin: 30px 0 0 0;
`

const Styles = {
  Row,
  ExtraId,
  Actions,
}

export default Styles
